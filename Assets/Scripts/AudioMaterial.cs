using System;
using UnityEngine;

public class AudioMaterial : MonoBehaviour
{
    public enum materialType
    {
        Stone,
        Wood,
        Metal,
        Water,
    }
    public materialType type;
}
